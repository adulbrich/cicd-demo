# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-push:
  jobs:
    - run: npm run build
    - run: npm test:e2e
    
pre-commit:
  parallel: true
  jobs:
    - run: npm run format
    - run: npm run lint

